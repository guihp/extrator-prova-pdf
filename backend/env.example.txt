# Copie este arquivo para .env e preencha com suas credenciais

# PostgreSQL (use URL completa ou variáveis individuais)
POSTGRES_URL=postgresql://usuario:senha@host:porta/database

# OU use variáveis individuais (se não usar POSTGRES_URL):
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=sua_senha
# POSTGRES_DB=postgres

# Google Gemini (obtenha em https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=sua_chave_gemini_aqui

# OpenAI (obtenha em https://platform.openai.com/api-keys)
OPENAI_API_KEY=sua_chave_openai_aqui

# Redis
REDIS_URL=redis://usuario:senha@host:porta/0
# Exemplo local: redis://localhost:6379/0
# Exemplo remoto: redis://default:senha@host:porta/0

# Configurações
UPLOAD_DIR=uploads
IMAGES_DIR=images
MAX_FILE_SIZE=10485760
BASE_URL=http://localhost:8000
# Em produção: BASE_URL=https://api.seudominio.com

